name: Task 6 Candidate Gate

on:
  push:
    branches:
      - verification/task6-green

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout exact Task 6 candidate
        uses: actions/checkout@v4
        with:
          ref: e345e8b6054dffcb075b0029272386373a022119
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install project
        run: python -m pip install -e ".[dev]"
      - name: Show exact HEAD
        run: git rev-parse HEAD
      - name: Task 6 pytest
        run: pytest tests/unit/source/test_gold.py tests/unit/source/test_openstax.py -q
      - name: Ruff
        run: ruff check src tests
      - name: MyPy
        run: mypy src
